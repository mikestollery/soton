#include <stdio.h>
#include <ctype.h>
#include <string.h>

void encipher(const char *p, char *c, const unsigned int offset);
void decipher(const char *p, char *c, const unsigned int offset);

int main () {

    char* message = "This is a sentence.";

    for char

    char* enciphered;
    int offset = 5;

    encipher(message, enciphered, offset);
}

void encipher(const char *p, char *c, const unsigned int offset) {
    int length = strlen(p);
    printf("length=%d\n", length);

    char enciphered[length + 1];

}

